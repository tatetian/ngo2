
// TODO: how to async I/O request to return?
// When we want to force exit a process, 

mod stream;
mod runtime;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
